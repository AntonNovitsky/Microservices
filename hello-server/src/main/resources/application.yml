spring:
  application:
    name: hello-server
  cloud:
    client:
      hostname: ${spring.application.name}

server:
  port: 8098

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
